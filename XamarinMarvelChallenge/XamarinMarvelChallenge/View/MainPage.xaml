<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="XamarinMarvelChallenge.View.MainPage">
    <ContentPage.Content>
        <StackLayout Padding="20">
            <Label Text="Welcome to Xamarin Marvel Challenge" FontSize="Large" 
                   FontAttributes="Bold" HorizontalTextAlignment="Center"/>

            <Label IsVisible="{Binding IsBusy}" Text="Please wait until the data is retrieved.." TextColor="Red" 
                   HorizontalTextAlignment="Center" HorizontalOptions="CenterAndExpand" VerticalOptions="CenterAndExpand"/>

            <Button IsVisible="{Binding IsNotBusy}" BackgroundColor="Red" TextColor="White" 
                    Text="Proceed to the app" Command="{Binding ProceedCommand}"
                    HorizontalOptions="CenterAndExpand" VerticalOptions="CenterAndExpand"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>